// Crops Module JavaScript
function initializeCropsGrid() {
    console.log('Crops Grid initialized');
}

if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', initializeCropsGrid);
} else {
    initializeCropsGrid();
}

